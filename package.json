{
  "name": "auth0-netlify-protected-content",
  "version": "1.0.0",
  "description": "Contenido HTML estático protegido con Auth0 y Netlify Functions",
  "main": "index.html",
  "scripts": {
    "dev": "node scripts/inject-env.js development && netlify dev",
    "build": "node scripts/inject-env.js production",
    "build:dev": "node scripts/inject-env.js development",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.0.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "keywords": [
    "auth0",
    "netlify",
    "functions",
    "authentication",
    "static-site"
  ],
  "author": "Tu Nombre",
  "license": "MIT"
}
